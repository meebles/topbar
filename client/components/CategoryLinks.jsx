import React from 'react';
import T from 'prop-types';

const CategoryLinks = ({ linksList }) => {
  //
};

CategoryLinks.propTypes = {
  linksList: T.arrayOf(T.string),
};

export default CategoryLinks;
